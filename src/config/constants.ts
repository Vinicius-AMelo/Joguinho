export const TILE_SIZE = 35

export enum EWalker {
  LEFT = 'ArrowLeft',
  RIGHT = 'ArrowRight',
  UP = 'ArrowUp',
  DOWN = 'ArrowDown',
}

export enum EPixels {
  FL = 0,
  WL = 1,
  TR = 2,
  MD = 3,
  DM = 4,
  CH = 5,
  HR = 6,
}
