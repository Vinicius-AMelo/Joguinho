export const GAME_SIZE = 960

export const TILE_SIZE = 48

export enum EWalker {
  LEFT = 'ArrowLeft',
  RIGHT = 'ArrowRight',
  UP = 'ArrowUp',
  DOWN = 'ArrowDown',
}

export enum EPixels {
  FL = 0,
  WL = 1,
  TR = 2,
  MD = 3,
  DM = 4,
  CH = 5,
  HR = 6,
  HL = 7,
}
